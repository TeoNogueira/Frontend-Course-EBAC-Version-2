git checkout .\nome_do_arquivo <<< desfazendo apenas commit

git checkout 

git checkout commit_hash


comandos linux:

 cat <<< verifica o que há dentro de um arquivo.

ls verifica o que há dentro da pasta (Recomendo usar ls -lias) para ver permissoes data etc...
